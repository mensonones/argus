# Dependencies
node_modules/

# Logs & env
*.log
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# TypeScript incremental build info
*.tsbuildinfo

# pnpm may materialize a local store when `npm run install:dsh` runs from here
.pnpm-store/

# Argus per-target memory / exports (written into the repo being reviewed)
.argus/

# Local evaluation runs may contain private reviewer transcripts.
eval/runs/

# NOTE: the plugin ships its compiled dist/ (committed) so it runs without a
# build step on install — do NOT ignore plugins/argus/dist.

# Private evaluation corpus (real diffs from private repos — never publish)
eval/corpus-private/
