# DSH lab observation and maintainer grouping decision

Recorded on 2026-09-13 for the user-provided 2026-09-12 run. This is a live
workflow observation, not an addition to the blinded pilot or a scored result.
Do not give this note to reviewers as task input or treat it as ground truth.

Host/model reported by the user: DSH, DeepSeek Flash 4.1. Exact host build,
model identifier/settings and installed Argus build were not independently
captured for this run. The report demonstrates the historical-match workflow
introduced in Argus 0.2.3; that does not establish the exact installed build.

Review target: synthetic argus-lab working tree versus main, baseline revision
`b89541f5` (short revision), dirty tree. Source report ID:
`a8c6b1dd-42b8-4ec7-a4cb-85274d6ecac8`. Raw report and execution artifacts remain
outside this repository; no source report or lab history was rewritten.

Observed from the saved report: four reviewers, seven candidates, zero rejected,
three duplicates merged, four final findings. Three established regressions
(owner-check removal, refund idempotency, batch-to-serial IO) retain the prior
baseline identities and PERSISTENT status. A fourth storage-surface finding is
NEW: newly identified against recorded history, not newly introduced in code.

The report describes a nonempty getMany-only adapter throwing because the code
calls get. This note does not independently re-execute that probe. The fourth
finding's reconciliation cites conflicting Challenger positions and the guide's
storage example to justify a split. That rationale needs independent human
adjudication; neither agreement nor the guide establishes independence.

## Adjudication questions

- Is the getMany-only surface a supported contract for nonempty input?
- Is its reachable failure an independent defect or another consequence of the
  already reported loadAccounts API regression? Compare causal preconditions and
  targeted controls, not just invariant labels, lenses or a shared fix.
- Does the retained canonical content accurately cover all verified consequences
  without speculative production costs or latency extrapolation?

Status at the initial observation: pending. No fourth ground-truth issue, false-positive label, precision,
recall or recall advantage is assigned. The three-regression expectation is a
known test set, not an assertion that all other findings must be false positives.

## Subsequent maintainer decision — 2026-09-13

The maintainer accepted the assistant's proposal to report one loadAccounts API
regression with two retained consequences: N serialized calls and the conditional
TypeError for a supported getMany-only adapter on nonempty input. For this diff,
the documented batch-to-individual operation replacement is the shared causal
change; keeping both consequences in one entry is actionable without losing
the functional failure. A shared fix alone is not the justification. Distinct
triggers remain acknowledged, so this is a scoped reporting/adjudication choice,
not a universal rule for IO/interface findings or proof of general model quality.

The later report `074a11d7-68e6-4b6a-994c-c9b2f92926ba` retained the interface
symptom in the batch-load canonical but labeled the old facet not redetected.
The requested runtime relationship instead records that historical facet as
incorporated, with explicit covered claims and rationale. No old report, finding
or lab snapshot is rewritten; no pilot issue labels or scored metrics change.
This note adds the accepted grouping decision to the originally pending record;
it does not claim a new independent execution of the probes or broad ground truth.

User-reported usage: approximately 1.5 million tokens and 8 minutes elapsed.
Token aggregation, cache accounting, pricing and retry breakdown are unknown;
these figures are not independently instrumented or monetized here. Comparisons
with OpenCode change model and harness together and do not isolate their effects.
